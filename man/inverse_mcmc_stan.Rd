% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inversion_mcmc_stan.R
\name{inverse_mcmc_stan}
\alias{inverse_mcmc_stan}
\title{SABER inverse model using Stan/HMC sampling}
\usage{
inverse_mcmc_stan(
  rrs,
  forward_model = "am03",
  par_inversed,
  prior = NULL,
  lower = NULL,
  upper = NULL,
  par_fixed = NULL,
  iterations = 2000,
  warmup = 1000,
  chains = 4,
  adapt_delta = 0.8,
  max_treedepth = 12,
  backend = c("pure_stan", "hybrid", "cpp_optimized"),
  return_fit = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{rrs}{data-frame with columns: wavelength [nm] and rrs_0m [1/sr]}

\item{forward_model}{Forward model name (default = "am03")}

\item{par_inversed}{Vector of parameter names to retrieve}

\item{prior}{List of prior specifications (optional, uses weakly informative defaults)}

\item{lower}{Numeric vector of lower bounds}

\item{upper}{Numeric vector of upper bounds}

\item{par_fixed}{Named list of fixed parameters (water_type, theta_sun, etc.)}

\item{iterations}{Number of post-warmup iterations (default = 2000)}

\item{warmup}{Number of warmup/adaptation iterations (default = 1000)}

\item{chains}{Number of MCMC chains (default = 4)}

\item{adapt_delta}{Target acceptance rate (0.8-0.99, higher = more accurate, slower)}

\item{max_treedepth}{Maximum tree depth for NUTS (default = 12)}

\item{backend}{Which Stan backend to use: "pure_stan" (default), "cpp_optimized",
or "hybrid". See Details.}

\item{return_fit}{Return full Stan fit object? (default = FALSE)}

\item{verbose}{Print Stan sampling progress? (default = TRUE)}
}
\value{
Named vector of MAP estimates and standard deviations.
If return_fit=TRUE, returns list with estimates and fit object.
}
\description{
Uses Hamiltonian Monte Carlo (HMC) with No-U-Turn Sampler (NUTS) via Stan.
Much more efficient than random-walk MCMC for complex posteriors, especially
in shallow dark waters with low SNR.
}
\details{
\strong{Why HMC/NUTS is better than DEzs/DREAMzs:}
\enumerate{
\item \strong{Gradient-guided exploration}: Uses log-posterior gradients to propose
efficient moves, avoiding random walk behavior.
\item \strong{Handles correlations}: Explores along posterior contours, not trapped
by parameter correlations (e.g., Chl vs a_g).
\item \strong{Fewer samples needed}: ~2000-5000 effective samples vs 30,000+ for DEzs.
\item \strong{Better for shallow waters}: Multi-modal posteriors (different benthic
mixtures) are explored more thoroughly.
\item \strong{Automatic tuning}: NUTS adapts step size and trajectory length.
}

\strong{When to increase adapt_delta:}
\itemize{
\item If you see "divergent transitions" warnings
\item Low SNR / dark waters: use 0.95-0.99
\item Start with 0.8, increase if needed
}

\strong{Stan Backend Options:}
\itemize{
\item \code{"pure_stan"} (default): All code in Stan language, fully vectorized.
Easy to modify, good performance (~10-20 min for 71 bands).
\item \code{"cpp_optimized"}: Uses external C++ functions (colleague's approach).
Fastest (~3-5 min), requires C++ header compilation.
\item \code{"hybrid"}: C++ for IOP calculation (bottleneck), Stan for forward model.
Balanced speed (~7-12 min) and flexibility. RECOMMENDED for development.
}
}
\references{
Hoffman, M. D., & Gelman, A. (2014). The No-U-Turn sampler: adaptively
setting path lengths in Hamiltonian Monte Carlo. JMLR, 15(1), 1593-1623.
}
