% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_parser.R
\name{compute_l2_products}
\alias{compute_l2_products}
\title{Compute L2 optical products from inversion results}
\usage{
compute_l2_products(
  rrs_df,
  cfg,
  result_mcmc = NULL,
  result_grad = NULL,
  compute_iop = TRUE,
  compute_kd = TRUE,
  compute_par = FALSE,
  plot_rrs = TRUE,
  plot_iop = TRUE
)
}
\arguments{
\item{rrs_df}{Data frame with columns \code{wavelength} [nm] and
\code{rrs_0m} [sr\eqn{^{-1}}].}

\item{cfg}{List returned by \code{\link[=make_inversion_params]{make_inversion_params()}}.}

\item{result_mcmc}{Named numeric vector from \code{\link[=inverse_mcmc]{inverse_mcmc()}}, or
\code{NULL}.}

\item{result_grad}{Named numeric vector from \code{\link[=inverse_gradient]{inverse_gradient()}}, or
\code{NULL}.}

\item{compute_iop}{Return full IOP spectra (\code{a}, \code{bb},
\code{a_phy}, \code{a_dg}, \code{bb_p})?  Default \code{TRUE}.}

\item{compute_kd}{Return diffuse attenuation
\eqn{K_d = (a + b_b)/0.8}?  Default \code{TRUE}.}

\item{compute_par}{Compute PAR via the Gregg-Carder irradiance model?
Requires \code{lat}, \code{lon}, \code{date_time} in
\code{cfg$par_fixed}.  Default \code{FALSE}.}

\item{plot_rrs}{Build a ggplot2 Rrs reconstruction figure?  Default
\code{TRUE}.}

\item{plot_iop}{Build a 4-panel IOP + \eqn{K_d} figure?  Default
\code{TRUE}.}
}
\value{
Named list with elements \code{wavelength}, \code{rrs_obs},
\code{rrs_recon}, \code{gof}, \code{par_retrieved}; optionally
\code{iop}, \code{Kd}, \code{par}, and \code{plots}.
}
\description{
Reconstruct Rrs and compute derived L2 products from inversion results
}
\details{
Re-runs the forward model at the retrieved parameters, computes
goodness-of-fit metrics (RMSE, MAPE, R\eqn{^2}), and optionally derives
IOPs, \eqn{K_d}, PAR, and diagnostic plots.
}
