% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_parser.R
\name{make_inversion_params}
\alias{make_inversion_params}
\title{Build a per-pixel inversion configuration}
\usage{
make_inversion_params(
  depth_m,
  rrs_df,
  par_inv = .PAR_INVERSED_DEEP,
  lower = NULL,
  upper = NULL,
  fixed = NULL,
  sicf = FALSE,
  sicf_model = "analytical",
  depth_integration = TRUE,
  water_type = 2,
  lat = NULL,
  lon = NULL,
  date_time = NULL,
  theta_sun = 30,
  depth_threshold = 20,
  optical_depth_threshold = 4,
  red_green_min = 0.1,
  force_shallow = FALSE
)
}
\arguments{
\item{depth_m}{Bathymetric depth [m, positive down], or \code{NULL} /
\code{NA} to force optically-deep treatment.}

\item{rrs_df}{Data frame with columns \code{wavelength} and
\code{rrs_0m} [sr\eqn{^{-1}}].}

\item{par_inv}{Character vector of OAC parameters to invert.  Default
\code{c("chl", "a_dg_440", "bb_p_550")}.  Additional invertible OAC
parameters: \code{a_dg_s}, \code{bb_p_gamma}, \code{phi_f}.
\strong{Do not} include shallow-branch parameters (\code{h_w},
\code{mix_sand}, \code{r_rs_b_*}) here — they are appended automatically.
Use \code{fixed} to pin any of them to a constant value.}

\item{lower}{Optional named numeric lower-bound overrides (applies to
both OAC and auto-appended shallow params).}

\item{upper}{Optional named numeric upper-bound overrides (applies to
both OAC and auto-appended shallow params).}

\item{fixed}{Optional named list of parameters to hold constant (remove
from the inverted set and pass to \code{par_fixed}).  Can target any
parameter, e.g. \code{list(h_w = 5, a_dg_s = 0.015)}.  Parameters listed
here must not also appear in \code{par_inv}.}

\item{sicf}{Logical.  Use the SICF forward model (\code{"am03_sicf"})
and automatically promote \code{phi_f} into \code{par_inv}?
Default \code{FALSE}.}

\item{sicf_model}{SICF variant: \code{"analytical"} (default) or
\code{"semi_analytical"}.}

\item{depth_integration}{Logical.  Depth-integrated SICF?  Default
\code{TRUE}.}

\item{water_type}{Integer coastal-water type for the forward model.
Default \code{2}.}

\item{lat, lon, date_time}{Latitude \link{°N}, longitude \link{°E}, and UTC
\code{POSIXct} timestamp.  Required when \code{sicf = TRUE}.}

\item{theta_sun}{Solar zenith angle \link{°}.  Default \code{30}.}

\item{depth_threshold}{Depth \link{m} threshold for shallow detection.
Default \code{20}.}

\item{optical_depth_threshold}{\eqn{2 K_d z} threshold for shallow
detection.  Default \code{4.0}.}

\item{red_green_min}{Minimum \eqn{R_{rs}(665)/R_{rs}(560)} ratio for
benthic-signal detection.  Default \code{0.10}.}

\item{force_shallow}{Logical.  If \code{TRUE}, skip the spectral/depth
shallow-detection test and always use the shallow forward model.
Default \code{FALSE}.}
}
\value{
Named list: \code{shallow}, \code{h_w}, \code{fwd_model},
\code{par_inv}, \code{lower_b}, \code{upper_b}, \code{init_val},
\code{bt_prior}, \code{log_prior_fn}, \code{par_fixed}.
}
\description{
Constructs bounds, initial values, and a BayesianTools prior for a single
pixel.  Automatically selects optically-shallow treatment and appends
benthic fraction parameters when depth and spectral criteria are met.
Benthic reflectance classes must be loaded beforehand with
\code{\link{select_benthic_classes}}.
}
\examples{
\dontrun{
cfg <- make_inversion_params(depth_m = NA, rrs_df = rrs_df)
est <- inverse_mcmc(rrs_df, cfg$fwd_model, cfg$par_inv,
  prior = cfg$bt_prior, par_fixed = cfg$par_fixed)
}

}
