% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_gregg_carder.R
\name{.gc_irradiance}
\alias{.gc_irradiance}
\title{Gregg & Carder spectral solar irradiance at the sea surface (0+)}
\usage{
.gc_irradiance(
  jday,
  rlon,
  rlat,
  lam.sel = 350:700,
  the = -99,
  hr = -99,
  Vi = 15,
  am = 1,
  wsm = 4,
  ws = 6,
  pres = 1013.25,
  rh = 80,
  wv = 1.5
)
}
\arguments{
\item{jday}{Julian day (integer, 1-365)}

\item{rlon}{Longitude, W negative (decimal degrees)}

\item{rlat}{Latitude,  N positive (decimal degrees)}

\item{lam.sel}{Wavelength vector (nm, default 350:700)}

\item{the}{Solar zenith angle (degrees); \code{-99} to compute from
\code{hr}/\code{rlon}/\code{rlat}}

\item{hr}{Time in decimal hours GMT; required when \code{the < -90}}

\item{Vi}{Visibility (km, default 15)}

\item{am}{Air mass type 1-10 (default 1)}

\item{wsm}{Mean wind speed m/s (default 4)}

\item{ws}{Current wind speed m/s (default 6)}

\item{pres}{Atmospheric pressure mb (default 1013.25)}

\item{rh}{Relative humidity \% (default 80)}

\item{wv}{Water vapour cm (default 1.5)}
}
\value{
List: \code{lam}, \code{Edir} (direct), \code{Edif} (diffuse),
\code{Ed} = Edir + Edif (all in W/m2/nm at 0+), plus
\code{sunzen_deg} (resolved solar zenith angle in degrees).
}
\description{
Computes direct and diffuse downwelling spectral irradiance just above
the sea surface (0+) using the Gregg & Carder (1990) maritime atmosphere
model.  Fresnel surface reflectance is NOT applied here — call
\code{.gc_sfcrfl} separately and apply to convert to subsurface (0-).
Uses the internal \code{gc_data} spectral reference table — no external
package required.
}
\keyword{internal}
